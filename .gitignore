# Claude / editor session data
.claude/

# Reference material and OS junk (not part of the project)
*.pdf
*.lnk

# Personal review/understanding docs — kept local only, never pushed. Everything you drop in
# here (report drafts, notes, interview prep, old plan versions) stays out of git automatically.
review_prep/
